name: Dispatch Event to Cypress-Cabeceira

on:
  push:
    branches:
      - main 

jobs:
  dispatch:
    runs-on: ubuntu-latest

    steps:
      - name: Dispatch repository B event
        uses: peter-evans/repository-dispatch@v2
        with:
          token: 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
          repository: JoaoVitorSantDrade/cypress-cabeceira
          event-type: trigger-event
